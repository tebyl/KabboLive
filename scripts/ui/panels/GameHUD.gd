extends CanvasLayer
## HUD principal en sala: top bar, bottom bar, side panel, objetivo diario.
## Stub — lógica migrada desde GameUI.build_premium_*() en Fase 4.

signal change_room_requested()
signal open_shop_requested()
signal open_pause_requested()
signal open_help_requested()
signal explore_requested()
signal decorate_requested()
signal open_inventory_requested()
signal open_profile_requested()

# TODO Fase 4: extraer build_premium_top_bar / bottom_bar / side_panel / objective_panel de GameUI.gd
